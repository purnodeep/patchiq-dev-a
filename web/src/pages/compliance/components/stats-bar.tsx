// This component is superseded by the inline HealthStrip in framework.tsx (M3 UI overhaul).
// Kept as a no-op export to avoid breaking any potential future imports.
export function StatsBar() {
  return null;
}
