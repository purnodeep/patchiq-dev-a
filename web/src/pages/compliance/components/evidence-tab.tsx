export function EvidenceTab() {
  return (
    <div
      style={{
        textAlign: 'center',
        padding: '48px 24px',
        color: 'var(--text-muted)',
      }}
    >
      <p
        style={{
          fontFamily: 'var(--font-sans)',
          fontSize: 14,
          fontWeight: 600,
          marginBottom: 8,
          color: 'var(--text-secondary)',
        }}
      >
        Evidence &amp; Reports
      </p>
      <p style={{ fontFamily: 'var(--font-sans)', fontSize: 12 }}>
        Compliance report generation will be available in a future release.
      </p>
    </div>
  );
}
