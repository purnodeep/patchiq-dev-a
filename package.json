{
  "name": "patchiq",
  "private": true,
  "type": "module",
  "scripts": {
    "dev:web": "pnpm --filter @patchiq/web dev",
    "dev:hub": "pnpm --filter @patchiq/web-hub dev",
    "dev:agent": "pnpm --filter @patchiq/web-agent dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.0.2",
    "prettier": "^3.8.1",
    "typescript-eslint": "^8.56.1"
  }
}
